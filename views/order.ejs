<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Order Details</title>
</head>
<body>
    <h1>Order Details</h1>
    <h2>Order ID: <%= order.id %></h2>
    <p>Placed by: <%= order.user %></p>
    <p>Order Time: <%= order.timestamp.toLocaleString() %></p>
    <h3>Items:</h3>
    <ul>
        <% order.items.forEach(item => { %>
            <li>
                <%= item.snackName %> - Quantity: <%= item.quantity %> - Total: $<%= (item.price * item.quantity).toFixed(2) %>
            </li>
        <% }); %>
    </ul>
    <h3>Total Price: $<%= order.items.reduce((total, item) => total + (item.price * item.quantity), 0).toFixed(2) %></h3>
    <a href="/">Back to Home</a>
</body>
</html>
